{
  "name": "claude-worktree-monitor-plugin",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "pretest": "pnpm build",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.6.2",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
